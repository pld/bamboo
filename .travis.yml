language: python
python:
  - "2.7"
before_install:
  - sudo apt-get install gfortran libatlas-base-dev
install:
  - pip install numpy --use-mirrors
  - pip install -r requirements.pip --use-mirrors
script: cd bamboo && nosetests
services:
  - mongodb
notifications:
  irc: "irc.freenode.org#moditest"
